configuration file: file_server.xml

1. com.topcoder.processor.ipserver
	fileserver_address: the ipserver address
	fileserver_port: the ipserver port 

Notes: 
	a. make sure auto_screening tool use the same server address and port
	b. make sure the port is available and won't be used by others app

2. com.topcoder.onlinereview.SubmissionFileSystemServerTool	
	idGenerator_name : the upload file handler id generator name
	server_dir : the file server dir to store upload file
	
3. com.topcoder.db.connectionfactory.DBConnectionFactoryImpl
	used for file_system_id_seq only

4. create id_seq script
	file_system_id_seq.sql

