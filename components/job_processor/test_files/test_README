1. Modify the "build.xml" to update the value of "junit", maybe you also need to configure the path for dependencies.
2. During the test case running, it may popup two error dialog, just ignore them.